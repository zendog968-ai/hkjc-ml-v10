# V10.2 沙田濕地 1,200 米：T-15／T-5 官方賠率快照回測可用性稽核

**作者：** Manus AI
**目標樣本：** 沙田、1,200 米、好地至黏地／黏地／軟地／濕慢地的時間外測試賽事，合共 17 場
**稽核目的：** 只以賽前已可得的香港賽馬會官方獨贏賠率快照，重算模型首選的 EV、固定注額 ROI、回撤與市場比較 [1] [2]

## 結論

本次**不能產生真實 T-15 或 T-5 回測的 EV／ROI 數字**，原因是目標 17 場歷史賽事沒有可匹配的正式賽前快照。系統已嚴格拒絕以賽後最終賠率代替，故結果應記為 **N/A（資料不足）**，而不是 0%。

| 快照時點 | 目標場次 | 可找到的正式匹配快照 | 完整可用賠率覆蓋 | 可回測注數 | EV／ROI |
|---|---:|---:|---:|---:|---|
| T-15 | 17 | 0 | 0.00% | 0 | N/A |
| T-5 | 17 | 0 | 0.00% | 0 | N/A |

> 正確結論不是「模型在真實市場環境下表現為零」，而是「現有資料不能衡量真實市場環境下的表現」。任何用最終賠率得出的 +24.71% 歷史代理 ROI，都不能替代這個缺失的賽前回測。

## 1. 已核實的快照覆蓋

![賽前快照覆蓋](v102_st_wet_1200_prerace_backtest/01_prerace_snapshot_coverage.png)

稽核首先檢查預賽排程的預設儲存根目錄 `runtime/pre_race`。該目錄沒有找到任何 `v10.2_odds_snapshot` 快照；主 SQLite 資料庫亦不包含賽前賠率快照表。資料庫只有賽事、出賽、特徵、ELO 狀態、裝備及更新稽核相關資料表，沒有可用作 T-15／T-5 的價格歷史。

進一步掃描整個專案，只找到 1 個符合快照 schema 的 JSON：`v102_snapshot_test/t15.json`。它是測試產物，標示為跑馬地、2026/07/15、第 1 場，且捕捉時間為 2026/08/14；它不符合任何目標沙田濕地 1,200 米賽事。專案中沒有 T-5 schema 快照。因此，該檔案已被嚴格排除，而非錯誤納入回測。

| 稽核位置／來源 | 尋獲狀況 | 是否可用於目標 17 場 |
|---|---|---|
| `runtime/pre_race` | 0 個正式快照檔 | 否 |
| SQLite 主資料庫 | 沒有賽前賠率快照表 | 否 |
| 全專案 JSON 掃描 | 1 個 T-15 測試快照 | 否；馬場、日期與場次均不匹配 |
| T-5 快照 | 0 個 | 否 |

## 2. 回測完整性規則

為防止任何形式的事後賠率洩漏，新的回測程式只接受同時符合以下條件的快照列：

| 核對項目 | 必要條件 | 防止的錯誤 |
|---|---|---|
| 快照架構 | `schema_version = v10.2_odds_snapshot` | 把舊覆蓋檔或人工測試檔當成正式快照。 |
| 時點標籤 | `T_MINUS_15` 或 `T_MINUS_5` | 混合其他時間點的價格。 |
| 賽事身分 | 賽日、馬場、場次與模型首選完全一致 | 把不同場次或不同馬場的報價接入。 |
| 快照狀態 | `status = complete` | 使用降級模式、空值、SCR 或爬取失敗資料。 |
| 獨贏價格 | 大於 1.0 的可解析官方賠率 | 把空值、0、退出或未有賠率視為成交價格。 |
| 模型資料 | 固定的時間外預測首選 | 不以賽果或最終賠率重選馬匹。 |

程式不會以歷史最終獨贏賠率補空。這項限制是必要的，因為最終賠率在投注池結算前不可知；若用其取代 T-15／T-5，就會把事後市場資訊帶入回測。

## 3. 為何無法回補目標歷史樣本

目標 17 場來自 2026-02-25 至 2026-07-15 的時間外評估期。T-15／T-5 排程器已具備在賽前保存檔案的機制：每場應寫出 `odds_t_minus_15.json` 與 `odds_t_minus_5.json`，並且包含 UTC 捕捉時間、賽日、馬場、場次、賠率、狀態與標籤。可是，這項機制不會自動回補已結束賽事的過去賽前報價。

香港賽馬會的歷史賽果可核實最終結果與最終市場資料，但不能由此重建某一匹馬在開跑前 15 或 5 分鐘的可得價格。因此，不能在資料缺口下誠實地給出「真實 T-15／T-5 ROI」。[1] [2]

## 4. 已完成的可重跑回測器

新增的 `backtest_v102_st_wet_1200_prerace_odds.py` 已經建立，日後有正式快照後可直接執行：

```bash
cd /home/ubuntu/hkjc_v10_database
python3 backtest_v102_st_wet_1200_prerace_odds.py \
  --db hkjc_last_season.sqlite \
  --predictions v102_multiseason_backtest_predictions.csv \
  --snapshot-root runtime/pre_race \
  --output-dir v102_st_wet_1200_prerace_backtest
```

當覆蓋可用時，程式會按 T-15 與 T-5 分開輸出模型首選的可用注數、命中率、以快照賠率計算的單注 EV、固定注額派彩、淨損益與 ROI。現時輸出為 0 場覆蓋與 `N/A`，符合資料完整性要求。

## 5. 下一步資料治理建議

| 優先度 | 行動 | 完成標準 |
|---|---|---|
| 高 | 每場保留 T-15／T-5 完整快照及 metadata。 | 每個 JSON 的 `status = complete`，賽事身分完整、獨贏賠率可解析。 |
| 高 | 保存 T-5 時模型預測 CSV／JSON及篩選報告。 | 能把同一時間的模型首選、賠率與最終賽果一對一連接。 |
| 高 | 任何缺快照賽事都記作「未覆蓋」，而非使用最終賠率補值。 | 回測樣本數、覆蓋率與缺失原因在報告顯示。 |
| 中 | 累積至少 30 場沙田濕地 1,200 米、且同時有 T-15 與 T-5 的完整樣本。 | 分別報告兩個時間點的 EV、ROI、回撤及模型／市場一致率。 |
| 中 | 以滾動、未參與模型設計的未來賽事驗證。 | 避免把同一段回測期用於反覆調參後再報告表現。 |

## References

[1] [香港賽馬會：所有場次賽果](https://racing.hkjc.com/zh-hk/local/information/resultsall)

[2] [香港賽馬會：單場賽果](https://racing.hkjc.com/zh-hk/local/information/localresults)

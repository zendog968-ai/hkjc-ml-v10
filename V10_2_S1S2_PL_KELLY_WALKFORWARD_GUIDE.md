# V10.2 海外 S1/S2：Plackett–Luce 與保守 Kelly 大型回測指南

> **用途：** 本指南提供一套可重跑的研究框架，用於評估海外 S1/S2 賽事中、已保存的 Plackett–Luce 機率與 T-15／T-5 官方賠率快照能否支持正期望值及可承受的回撤。
> **風險聲明：** EV、Kelly 及任何歷史 ROI 均是模型研究指標，不代表未來收益或無損失保證。賽馬回報高度分散，最終決策與風險由使用者自行承擔。

## 1. 先判斷能否開始回測

大型回測的第一個輸出不應是 ROI，而是**資料覆蓋率**。每一筆候選都必須同時具有模型生成時間、T-15 或 T-5 官方捕捉時間、同一馬匹／市場的賽前可得報價，以及賽後官方結算。只要其中一項缺失，該筆應保留於排除清單而不參與資本曲線。

| 必需欄位 | 用途 | 不合格時的處理 |
|---|---|---|
| `model_generated_at_utc` | 證明模型在價格快照前已固定。 | 生成時間晚於快照時排除。 |
| `captured_at_utc`、`snapshot_label` | 證明價格是 T-15 或 T-5 同場同馬的可得價格。 | 不是 `T_MINUS_15`／`T_MINUS_5` 或無 UTC 時間時排除。 |
| `snapshot_status=complete` | 排除空白、SCR、降級或不完整賠率。 | 排除。 |
| `predicted_probability` | Plackett–Luce 導出的 Win／Place／組合機率。 | 非 `(0,1)` 時排除。 |
| `captured_payout_multiple` | 賽前顯示的連本金回報倍數。 | 小於等於 1 或不明時排除。 |
| `settled_gross_return_per_unit` | 賽後官方結算的每單位總回報；落敗為 0。 | 未結算時排除。 |

現時 V10.2 海外 archive 尚未累積合資格的真實 S1/S2 官方 T-15／T-5 價格及官方結果組合列。因此，在正式資料進來以前，執行器會輸出 `N/A_no_eligible_prerace_settled_rows`；這是正確的稽核結果，而不是策略失敗或零回報。

## 2. Plackett–Luce 機率與賽前 EV

S1/S2 預測器把場內每匹馬的冷啟動強度正規化，再以向量化 Plackett–Luce 抽樣估計頭名及位置機率。任何市場候選都必須由當時已固定的機率產生，然後才與相同時間點的價格連接。

若 HKJC 顯示的 `captured_payout_multiple` 已是連本金的總回報倍數，單位期望值為：

> **EV** = `p × O − 1`

其中 `p` 是賽前模型機率，`O` 是同一 T-15／T-5 快照中的總回報倍數。不要再對 `O` 額外扣除抽水，亦不可用最終賠率或正式派彩替代 `O`。模型校準必須按時間序列完成：以前季度的結果只可校準後一季度，不可用全樣本重新校準舊候選。

## 3. 保守 Kelly 與同場資本上限

對正 EV 的單一獨立候選，完整 Kelly 分數為：

> `f_full = max(0, (p × (O − 1) − (1 − p)) / (O − 1))`

V10.2 不直接採用完整 Kelly，而是以研究配置比例 `c` 縮放：

> `f_raw = c × f_full`，其中 `c ∈ {0.125, 0.25, 0.50}`

然後先套用**單一候選上限**，再套用**同場組合上限**。這避免多個高度相關的同場 Win／Place 或組合候選被誤當成獨立風險。若同場候選的 `f_raw` 合計高於場次上限，所有同場 fraction 按比例縮小；同場所有注項都按同一個場前資本計算，整場結算後才更新下一場資本。

預設研究參數為：單一候選上限 1%、同場總上限 2%、fractional Kelly 候選為 1/8、1/4、1/2。這是為壓力測試提供的標準化起點，而非個人化下注建議；實際研究應另行比較更低上限與不使用 Kelly 的固定注額基準。

## 4. 走步季度驗證，而非事後調參

參數不可用完整歷史樣本挑選後再回報同一樣本的最佳 ROI。正確做法是逐季度走步：以所有先前季度選擇 `kelly_scale`，再把選中的配置固定套用於下一季度。V10.2 以香港賽季季度分組：9–11 月為 Q1、12–2 月為 Q2、3–5 月為 Q3、6–8 月為 Q4。

訓練期內，系統只有在至少 30 筆合資格候選時才選擇 scale。選擇函數以對數資本成長為主，同時對較深最大回撤施加懲罰；測試季度完全不參與選擇。這並不能消除模型選擇偏差，但比全期事後最佳化更能顯示策略對不同賽期的穩健性。

| 輸出 | 應如何解讀 |
|---|---|
| `exclusion_summary.csv` | 最優先檢查。模型晚於快照、快照不完整與未結算列不得藏在 ROI 外。 |
| `kelly_parameter_surface.csv` | 顯示各固定 Kelly scale 的全期研究面；只能作敏感度參考，不是最終選擇依據。 |
| `walkforward_quarter_summary.csv` | 每個真正未來季度的固定 scale、可用注數、ROI、最終資本與最大回撤。 |
| `walkforward_details.csv` | 逐場／逐候選的賽前 EV、stake、賽後淨回報與資本曲線審計。 |
| `walkforward_summary.json` | 資料完整性、季度範圍與 `N/A`／探索性標記。 |

## 5. 指令範例

先以日期分區、不可覆寫的 ledger 寫入候選與結算資料。例如 ledger 可置於 `archive/overseas_prerace_ledger/2026-09/`；每一列都必須已通過快照來源驗證。

```bash
cd /home/ubuntu/hkjc_v10_database

python3 analyze_s1s2_pl_kelly_walkforward.py \
  --ledger-glob 'archive/overseas_prerace_ledger/**/*.csv' \
  --output-dir v102_s1s2_pl_kelly_walkforward \
  --initial-bankroll 100 \
  --kelly-scales 0.125,0.25,0.5 \
  --max-single-fraction 0.01 \
  --max-race-fraction 0.02 \
  --min-ev 0.00 \
  --min-train-bets 30
```

應把 T-15 與 T-5 拆成兩個不同的 ledger glob／輸出目錄，絕不能混合比較。每一個海外市場、場地、賽區或冷啟動 tier 的細分，未滿 15 筆已結算候選時均應標示為探索性；若要將季度穩健性作為模型部署閘門，建議至少持續累積 30 筆完整、同質候選後再評估。

## 6. 長期優化的正確目標

回測不應只最大化樣本內 ROI。較嚴格的部署門檻應同時檢查：T-15／T-5 覆蓋率、時間外校準、有效注數、走步季度結果、最大回撤、最長回撤期、移除最大單一正回報後的結果、最差季度，以及價格快照時間偏差。若正 ROI 只由少數高派彩勝出、單一賽區或單一季度貢獻，應把策略視為脆弱，而非提高 Kelly scale。

當策略有完整歷史資料後，最先比較的基準應是固定小額、零 Kelly，以及 1/8 Kelly；若 1/4 或 1/2 Kelly 只增加回撤而沒有改善走步季度的中位數結果，較低 fraction 通常是較可審計的研究配置。任何實際配置改動都應先在其後未見季度觀察，而不是在已回測資料上即時採用。

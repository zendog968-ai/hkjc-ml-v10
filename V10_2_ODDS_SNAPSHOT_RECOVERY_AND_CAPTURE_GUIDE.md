# V10.2 Odds Snapshot 修復與持續保存指南

**作者：** Manus AI
**目的：** 修復可證明來源的歷史 T-15／T-5 賠率資料，並建立可持續保存、可稽核、可重跑的賽前快照流程。
**適用範圍：** 香港賽馬會本地賽事的獨贏／位置賠率；V10.2 賽前模型、EV、固定注額回測與市場比較。[1] [2]

## 1. 先界定「修復」的正確含義

歷史賽果頁可用於核實賽果、最終賠率或派彩，但**不能誠實地重建一匹馬在開跑前 15 分鐘或 5 分鐘的已知價格**。因此，快照修復只能從原先已保存的賽前檔案、資料庫、雲端備份、交易紀錄或具原始時間戳記的外部資料檔開始；不能以最終賠率、賽後派彩、新聞截圖或記憶中的價格補值。

> V10.2 的真實 T-15／T-5 回測是「在該時點模型已知的機率」乘以「該時點可得的官方賠率」。任何賽後資料替代都會形成時間穿越，令 EV、ROI 與回撤無法用於實證。

目前已知的沙田濕地 1,200 米 17 場樣本沒有可匹配的正式快照，故 T-15／T-5 EV、ROI 與回撤必須保持 `N/A`。現存的測試 JSON 已經被排除，不能混入正式資料庫。

## 2. 三類可行資料來源與不可接受替代品

| 類別 | 可否用於真實 T-15／T-5 回測 | 最低必備證據 | 使用方式 |
|---|---|---|---|
| 自行保存的排程輸出、備份、物件儲存或資料庫紀錄 | 可以 | 賽日、馬場、場次、`T_MINUS_15`／`T_MINUS_5` 標籤、UTC 捕捉時間、逐馬獨贏賠率與原始檔案。 | 經匯入器驗證、雜湊去重及逐馬對照後，納入正式回測。 |
| 具授權、可審計的第三方逐分鐘賠率檔 | 條件式可以 | 原始供應商、授權、時區、逐筆時間戳記、賽事及馬匹唯一身分。 | 只有剛好落在目標時點或可被明確標為「最接近前一筆」的記錄才可用；須保留來源與延遲。 |
| 網頁封存、截圖、人工筆記或不明來源 CSV | 僅研究參考 | 至少可追溯 URL／生成時間；通常欠缺完整逐馬覆蓋。 | 不得標記為正式 T-15／T-5；可放入 `research_only` 區，不能參與 ROI。 |
| 賽後最終獨贏賠率、派彩或結果頁 | **不可以** | 無法證明賽前可得時間。 | 只可作最終市場基準；不得用來補 T-15／T-5 空值。 |

對外部檔案而言，最重要的不是檔案名稱，而是**時間可驗證性**。例如一個標示「開跑前」但沒有捕捉時間或時區的 CSV，不應被升格為 T-15；一個 14 分 42 秒前取得的價格，應保留實際時間與 `capture_delta_seconds`，而不是假裝精確等於 15 分鐘。

## 3. 持續保存方案：應按運行環境選擇

T-15／T-5 工作是每場兩次、屬確定性擷取與寫檔流程；它不需要每次啟動大型分析工作。下表列出可行選擇，應按既有運行環境、成本與是否要保留現有 Python／Chromium 取數程式決定。

| 方案 | 使用者體驗與實作方式 | 取捨 | 成本 | 設置複雜度 |
|---|---|---|---|---|
| 自己已有的長開 Linux／桌面電腦 | 以每分鐘排程執行現有 `pre_race_scheduler.py`；保留現有 Python、Playwright、Chromium 與 SQLite。 | 不需重寫抓取器，但電腦必須在賽日持續開機、網絡穩定。 | 若硬件已存在，邊際成本低；需自行承擔電力與維護。 | 中等 |
| 受管理的排程服務 | 將抓取、儲存與稽核重寫為受管理的後端定時工作及資料庫流程。 | 維護較少，但必須先驗證服務能安全支援公開頁面渲染或改寫為相容取數方式。 | 可由低用量／使用量計費開始，視供應商而定。 | 較高 |
| 獨立的持續 Linux 主機 | 把現有 V10.2 Python／Chromium 排程原樣放到持續運行的 Linux 環境，以 cron 與持久磁碟保存快照。 | 最貼近現有程式，可靠性較高；需管理主機、更新與備份。 | 需付主機費用，依供應商與規格而定。 | 中等 |

上述三種都能達到資料保存目的。若保留現有 Python／Playwright／Chromium 實作是優先事項，第一或第三種的改動最小；若同時希望有資料管理介面、參數編輯和集中報表，第二種較適合，但需要額外重構。應先選定其中一種，再進行部署；不宜在預設會休眠的工作環境中假定排程會跨賽日可靠執行。

## 4. 正式資料模型與不可變原則

已新增 `schema_prerace_odds_snapshots.sql`，建立以下三個物件：

| 物件 | 內容 | 防止的問題 |
|---|---|---|
| `pre_race_odds_snapshots` | 每一個捕捉事件的賽事身分、時點、狀態、來源、原始 JSON、檔案 SHA-256 與匯入時間。 | 重複匯入、來源不明、未能追溯原始資料。 |
| `pre_race_odds_runner_prices` | 對應 snapshot ID 的逐馬獨贏／位置賠率。 | 將不同賽事或不同時間點的馬匹價格混合。 |
| `v_pre_race_odds_complete` | 只顯示狀態為 `complete` 的可用價格。 | 降級、空值、SCR 或失敗抓取被拿去回測。 |

匯入器 `import_prerace_odds_snapshots.py` 已驗證測試資料庫中的完整快照可被一次寫入，並在第二次執行時依 SHA-256 自動去重。正式運作時應使用獨立的快照檔案庫，例如 `hkjc_odds_snapshot_archive.sqlite`；不要把它與模型訓練的主資料庫混放，從而降低不慎把賽前市場價格加入歷史特徵的風險。

## 5. 修復已保存歷史快照的標準程序

### 5.1 整理輸入檔案

先將每個可證明來源的 JSON 放入只讀匯入目錄，並保留原始檔名和路徑。每一個正式檔案至少要符合以下 JSON 契約：

```json
{
  "schema_version": "v10.2_odds_snapshot",
  "snapshot_label": "T_MINUS_15",
  "captured_at_utc": "2026-09-06T05:45:10+00:00",
  "race": {"race_date": "2026/09/06", "racecourse": "ST", "race_no": 3},
  "status": "complete",
  "odds": {
    "馬名甲": {"win": 4.8, "place": 1.8},
    "馬名乙": {"win": 9.5, "place": 2.7}
  }
}
```

`captured_at_utc` 必須為實際捕捉時間，不可由排程目標時間倒推填入。若外部來源只有相鄰時點，應保留原始時間並另附來源 manifest；在回測報告中把它稱為「最近可得快照」，不能稱為精確 T-15／T-5。

### 5.2 匯入與驗證

```bash
cd /home/ubuntu/hkjc_v10_database
python3 import_prerace_odds_snapshots.py \
  --db hkjc_odds_snapshot_archive.sqlite \
  --schema schema_prerace_odds_snapshots.sql \
  --input-root archive/incoming \
  --report archive/import_report.json
```

匯入器只會接受 `schema_version = v10.2_odds_snapshot`、標籤為 T-15 或 T-5、狀態為 `complete`、具有正確賽事身分且至少有一匹馬具可解析獨贏賠率的檔案。測試目錄預設被排除；如需測試可加 `--include-test`，但測試資料絕不能與正式資料庫或回測報告混合。

匯入後，應逐項核對 import report：掃描數、匯入數、已存在數、拒絕數、每個拒絕原因，以及快照／逐馬價格列數。任何標示為 `degraded`、缺馬、SCR、無時間戳記或馬名不能對應的檔案，應保留在隔離區而非強行補值。

## 6. 未來 T-15／T-5 的標準收集流程

現有 `pre_race_scheduler.py` 已能在每場指定時間寫入 `odds_t_minus_15.json` 與 `odds_t_minus_5.json`。修復的重點是讓它的輸出在賽後仍被保留、匯入、核對與備份，而非只停留在一次性工作目錄。

| 時點 | 必做動作 | 必存證據 | 不合格處理 |
|---|---|---|---|
| 排位公布後 | 建立 race card，記錄預定開跑時間與宣佈出賽馬匹。 | race card 原始 JSON、來源時間、賽事身分。 | 沒有完整名單則不排程。 |
| T-15 | 讀取 HKJC 公開獨贏／位置賠率並寫入快照。 | 原始 JSON、`captured_at_utc`、狀態、馬匹覆蓋、來源 URL。 | `degraded` 或大面積空值：記錄失敗，不可計入 T-15 回測。 |
| T-5 | 第二次取數、保存完整快照，再運行預測。 | T-5 JSON、prediction JSON／CSV、T-15 與 T-5 的關連路徑。 | 不以 T-15 冒充 T-5；該場的 T-5 覆蓋記為缺失。 |
| 賽後 | 增量取得官方賽果，匯入快照庫並跑完整性報告。 | 賽果來源、快照 SHA-256、匯入報告及覆蓋率。 | 結果未能一對一對應，隔離並人工覆核。 |

快照檔應額外增加下列審計欄位：`scheduled_start_hkt`、`target_offset_minutes`、`capture_delta_seconds`、`collector_version`、`race_card_sha256` 和 `source_url`。其中 `capture_delta_seconds` 是驗證核心：它衡量實際捕捉時間與預定 T-15／T-5 的偏差，讓回測可選擇嚴格容忍範圍，而不是相信檔名。

## 7. 真實回測的閘門

新增的 `backtest_v102_st_wet_1200_prerace_odds.py` 已執行嚴格過濾。只有同時通過下表所有項目的列才可產生 EV 與 ROI：

| 閘門 | 要求 |
|---|---|
| 賽事對應 | 賽日、馬場、場次與固定時間外模型首選完全匹配。 |
| 時點對應 | 標籤為 T-15 或 T-5，並保留實際捕捉 UTC 時間。 |
| 資料完整 | 快照 `status = complete`，模型首選有可用獨贏賠率（>1.0）。 |
| 時間完整性 | 捕捉時間在正式開跑前；缺失／延遲在報告中明示。 |
| 結果對應 | 使用其後官方賽果只作收益標記，不改變原模型首選或快照賠率。 |
| 不補值 | 不用最終賠率、T-15 價替代 T-5、或其他場次價補空。 |

當完整樣本累積後，應分開產生 T-15 與 T-5 報告，並同時顯示覆蓋率、樣本場次、平均 EV、固定注額 ROI、最大回撤、模型與市場熱門一致率、賠率變化與被排除的賽事。建議先累積至少 30 場同類條件的完整樣本，才比較兩個時間點的穩定差異。

## 8. 建議的執行順序

首先，尋找是否存在自己的舊排程目錄、備份磁碟、雲端檔案庫或具時間戳記的資料匯出。若找到，先依本指南的 JSON 契約和匯入器做來源稽核；不符合者只放研究隔離區。其次，選定一個能在賽日持續運行的部署選項，使日後每一場都保留 T-15、T-5、prediction、filter report 與賽後結果。最後，在完整快照累積後，才執行回測器並把先前的最終賠率代理報告與真正賽前回測報告清楚分開。

## References

[1] [香港賽馬會：賠率頁面](https://bet.hkjc.com/ch/racing/wpq/)

[2] [香港賽馬會：所有場次賽果](https://racing.hkjc.com/zh-hk/local/information/resultsall)

# 香港賽馬會公開即時賠率頁：來源核實

核實日期：2026-08-12（香港時間）

## 已核實事項

1. 香港賽馬會公開賽馬頁為 `https://bet.hkjc.com/ch/racing/wp`，搜尋結果與頁面描述均標示該服務提供實時更新的賽馬彩池賠率。[1]
2. 該頁為動態載入內容。於沒有可用賽事／動態資料未完成載入時，瀏覽器只顯示公共頁首、負責任博彩與登入部分，沒有可直接從靜態 HTML 提取的馬匹賠率資料。
3. 任何輔助程式應只嘗試讀取公開頁面或公開資料端點；若內容為空、結構改變、未開放或收到 HTTP 403／429，必須停止並回報，絕不可規避存取限制。
4. 由於系統當時沒有可用的本地即時賽事，本次不能以即時數值端到端驗證馬匹賠率；腳本需提供 `--fixture-json` 離線格式／`--dry-run` 以測試馬名對應及輸出契約。

## 設計後果

工具將採取「官方網頁公開內容 → 有資料則解析；無資料或限制則清楚失敗」的設計。讀取頻率預設至少間隔 60 秒，且一次執行只發出一個公開頁面請求。

## 參考資料

[1] [香港賽馬會：賽馬投注－賠率](https://bet.hkjc.com/ch/racing/wpq/)


## 動態頁面資料觀察

公開頁面在前端載入後可顯示馬號、中文馬名、檔位、負磅、騎師、練馬師、獨贏及位置賠率，以及更新時間。例如當時可見的一場轉播賽顯示第 4 場 10 匹馬的馬名與獨贏數值。前端配置顯示賠率頁的自動刷新間隔為 30 秒，並表明 `WIN` 是官方獨贏彩池名稱。[2]

瀏覽器資源觀察顯示該公開頁面向 `https://info.cld.hkjc.com/graphql/base/` 取得動態資料；但頁面快取不提供可直接重用的查詢內容。因此輔助腳本不會猜測或暴力探測 GraphQL 查詢，而會以公開 HTML 可見表格為主要解析目標，並提供離線 HTML 模式作可重現測試。

[2] [香港賽馬會：RacingConfig.js](https://bet.hkjc.com/Config/RacingConfig.js)

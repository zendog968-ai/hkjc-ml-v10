# V10 賽馬機器學習後台：操作與更新指南

**版本：V10 ML v1**
**作者：Manus AI**

本系統以香港賽馬會公開官方賽果與排位資料為基礎，將既有 V10 的可解釋勝率框架升級為「**ELO + 末段走勢代理 + LightGBM**」的可重跑分析流程。系統設計為兩層：首次建立完整資料庫及模型；其後每次只需要輸入日期、馬場及場次，即可產生場內相對勝率、模型熱度、與市場賠率比較及每注期望值。

> **重要限制：** 香港賽馬會公開賽果頁提供官方分段時間、沿途走位、完成時間及頭馬距離；但沒有一項可直接下載的「每匹馬實測末段 400 米時間」。因此，本系統的 `closing400_proxy` 是根據官方**最後一個沿途走位、最終名次及頭馬距離**計算的末段走勢代理指標，絕不可誤稱為儀器量度的個別馬匹 400 米分段。官方賽果資料來源見 [1]。

## 系統構成

| 元件 | 檔案 | 作用 |
|---|---|---|
| 官方賽果資料庫 | `hkjc_last_season.sqlite` | 保存 2025/26 馬季的賽日、場次及馬匹出賽資料。 |
| 限速更新器 | `hkjc_last_season_etl.py` | 以官方賽期表與單場賽果頁增量下載新資料；支援日期區間、限速、冷卻及續跑。 |
| 特徵工程 | `build_elo_features.py` | 建立無未來資料的馬匹／騎師／同條件 ELO、近績、走勢代理及騎練統計。 |
| 模型訓練 | `train_lightgbm.py` | 以時間序列切分訓練 LightGBM，進行機率校準並輸出 `horse_model.pkl`。 |
| 單場預測 | `predict.py` | 讀取新賽事 JSON，調用 `horse_model.pkl`，輸出勝率、熱度、EV、ELO 與樣本警示。 |
| 排位表輸入 | `fetch_hkjc_racecard.py` | 讀取已公開的官方排位表並生成 JSON；動態市場賠率可作為獨立覆蓋檔加入。 |
| 月度更新 | `monthly_update.py` | 串連增量抓取、資料清理、特徵重建與模型重訓。 |

## 一次性建置結果

現有資料庫涵蓋 2025/26 馬季 **88 個賽日、868 場紀錄與 10,947 筆馬匹出賽資料**。其中取消或宣布無效的場次被保留作稽核但排除於模型訓練；官方撤回狀態亦不會被誤計入勝負樣本。

ELO 特徵庫目前含 **10,381 筆可訓練的賽前特徵列**，覆蓋 1,427 匹馬及 41 名騎師。每一列均在該場開跑前建立，因此不會使用同場或其後賽果。ELO 更新採用場內相對名次分數，而同程同場 ELO 則以馬匹、馬場、路程和場地四個元素作索引。

LightGBM 的時間外測試使用最後 15% 賽日作測試集，而非隨機抽樣。測試集的首選勝出率為 **20.3%**、首三名包含頭馬比率為 **47.8%**；場內正規化機率的平均 Brier score 為 **0.890**，低於等機會基準 **0.919**。這些是歷史樣本的研究性驗證，不能解讀為未來回報、獲利或單場命中保證。

## 日常單場預測

最簡單的操作是以官方排位建立 `race_card.json`，再調用預測器。所有馬名、騎師及練馬師必須使用香港賽馬會公布的中文名稱，才能正確連接歷史資料。

```bash
# 1. 已公佈排位後，讀取官方排位表
python3 fetch_hkjc_racecard.py \
  --date 2026/09/06 --racecourse ST --race-no 3 \
  --output race_card.json

# 2. 如已有最新獨贏賠率，可準備 odds_overlay.json
#    格式：{"馬名": 賠率}；賠率為香港賽馬會顯示的獨贏賠率。

# 3. 生成預測
python3 predict.py \
  --db hkjc_last_season.sqlite \
  --model horse_model.pkl \
  --race-card race_card.json \
  --output-json prediction.json \
  --output-csv prediction.csv
```

`new_race_template.json` 與 `odds_overlay_template.json` 已提供輸入格式。若未能取得即時賠率，預測器仍會輸出勝率與模型熱度，但 `EV` 會留空；這比用過期賠率硬算 EV 更安全。

### 輸出欄位的正確解讀

| 欄位 | 定義 | 使用限制 |
|---|---|---|
| `predicted_win_probability` | 場內正規化後、合計為 100% 的模型相對勝出率。 | 只反映模型與可得歷史資料，不包含臨場意外、獸醫資訊及即時步速。 |
| `model_heat_index` | 相對場均機會的熱度；100 為場內平均。 | 不是市場人氣，也不是保證。 |
| `horse_elo` / `jockey_elo` | 截至資料庫最新賽果的馬匹／騎師相對評分。 | 新馬或轉倉馬樣本較少，應配合 `data_warning`。 |
| `closing400_proxy` | 末段走勢代理分數。 | 不是實測 400 米分段時間。 |
| `market_implied_probability` | 按香港賽馬會獨贏賠率換算的市場隱含機率。 | 會隨投注流量變動。 |
| `ev_per_unit` | `模型勝率 × (香港賠率 + 1) − 1`。 | 未計佣金、派彩規則、賠率跳動與模型誤差；正 EV 不等於必然值得下注。 |
| `suggestion` | 模型與樣本警示的摘要。 | 應視為研究提示，不是自動下注指令。 |

## 月度更新與重訓

每月完成最後一個賽日後，可執行以下指令。程式會由資料庫最後已記錄的賽日翌日開始抓取，保守限速，然後重新建立 ELO 特徵及訓練模型。

```bash
python3 monthly_update.py \
  --db hkjc_last_season.sqlite \
  --csv hkjc_last_season.csv \
  --end-date 2026-09-30
```

若只是調整模型參數或修正特徵，而不需要下載新資料：

```bash
python3 monthly_update.py --skip-fetch
```

## 如何讓每月更新自動運行

月度更新是完全確定性的工作，不需要每次人工判斷。部署時有兩種可行方式，請按您想要的管理方式選擇。

| 方式 | 適合情況 | 優點 | 取捨 |
|---|---|---|---|
| **固定每月在您的工作區執行** | 您只需每月一次更新，並在更新後自行查看報告。 | 設定最少；可保留完整執行記錄。 | 每次執行會啟動一次完整任務；不適合頻密抓取。 |
| **部署為可管理的資料分析小工具** | 您希望長期自動更新，且將來可能加入參數調整、預測歷史或報表頁面。 | 月度工作可在背景按排程運行，亦可設介面查看上次更新與模型結果。 | 首次部署較多設定，需要選定長期運行位置。 |

在啟用月度自動運行前，請先決定要採用哪一種方式；這會影響結果存放位置、是否需要管理介面，以及日後您如何更改更新日期或模型參數。

## 資料來源與責任聲明

本系統只使用香港賽馬會公開頁面的賽期、排位及賽果資料，抓取器會以單一工作執行緒、間隔延遲、定期冷卻及遇到 403／429 時停止的方式運作。不要提高併發或嘗試繞過網站限制。賽程、排位及賠率可以更改，應以香港賽馬會最後公布為準。[1] [2]

> 賽馬涉及不確定性和損失風險。本工具不會替您下注，也不會將模型的「較高機會」標籤視為穩膽。任何實際投注應由您自行作最終決定，並量力而為。

## 參考資料

[1] [香港賽馬會：本地賽果](https://racing.hkjc.com/zh-hk/local/information/localresults)

[2] [香港賽馬會：賽期表](https://racing.hkjc.com/zh-hk/local/information/fixture)

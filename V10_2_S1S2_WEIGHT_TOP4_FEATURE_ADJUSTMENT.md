# V10.2 S1/S2 負磅與近期前四頻率特徵調整

**版本：V10.2 Advanced Feature & Ensemble Edition**

**狀態：已實作；未校準的保守先驗**
**日期：2026-08-17**

## 1. 調整背景與決策範圍

2026-08-17 的 S1-9 賽後覆盤顯示，低負磅的 **14 號 Ladiva（117 磅、4 檔）跑入第三**，而原先作位置馬膽的 **2 號 Mearas Style 跑第四**；頭馬為 **4 號 Orange Vif**。這是一次有價值的個案檢討，但**不是**足以估計穩定因果效應的樣本。因此，本次不會根據單場結果大幅提高低磅或近績訊號，而是將兩者加入為可審計、可回退、幅度受嚴格限制的對數強度先驗。

> **核心原則：** 特徵僅使用預測產生時可驗證的官方排位資料及該時點之前已完成、已存檔的海外賽果；資料缺失時必須回退至中性值，不得以猜測補值。

| 檢討項目 | 本次結論 | 未採用的做法 |
|---|---|---|
| 負磅 | 用**場內平均負磅**比較，避免不同海外賽區的絕對磅數直接混用。 | 不會把 117 磅或任何固定數字設定為自動加分門檻。 |
| 近期表現 | 用預測時點前最新最多 5 場的「前四」記錄，並向場內基準大幅縮減。 | 不會把 1 場入位、或職業生涯總入位率視為強烈短期訊號。 |
| S1-9 覆盤 | 作為特徵遺漏檢查的觸發事件。 | 不會把 S1-9 的結果反向寫入當時的賽前預測或任何回測。 |

## 2. 新增特徵與公式

### 2.1 場內相對負磅：`weight_log_signal`

預測器會從 HKJC 官方海外排位卡已解析的 `weight_lbs` 計算可用馬匹的場內平均負磅。某馬的相對優勢定義為：

```text
weight_advantage_lbs = field_weight_mean - weight_lbs
weight_log_signal = clip((field_weight_mean - weight_lbs) / 10 × 0.04, -0.06, +0.06)
```

因此，低於場均 10 磅才得到 `+0.04` 的對數強度調整；極端差距的影響也被限制在 `±0.06`。若該馬沒有可驗證負磅，或全場無足夠有效負磅以建立場均值，則訊號固定為 `0.0`，審計詳情標示為 `no_verified_field_relative_weight`。

| 輸出欄位 | 含義 | 適用限制 |
|---|---|---|
| `weight_lbs` | 排位卡中的負磅。 | 僅保存可解析、正值的官方欄位。 |
| `field_weight_mean` | 同場所有有效負磅的平均。 | 不以缺失值填補。 |
| `weight_advantage_lbs` | 場均負磅減該馬負磅。正數代表相對輕磅。 | 只作場內比較。 |
| `weight_log_signal` | 上述受限對數強度調整。 | 未校準前上限為 `+0.06`、下限為 `-0.06`。 |

### 2.2 近期前四頻率：`recent_top4_log_signal`

系統會以馬名連接海外 archive，僅讀取下列條件全部成立的賽果：賽事狀態為 `completed`、有 `scheduled_start_utc`、開始時間嚴格早於產生預測時間、名次可解析、且不是已標示退出的出賽。資料依開跑時間倒序排列，只取**最近最多五場**。

對 field size 為 `F` 的賽事，先以 `b = min(4 / max(F, 4), 1)` 建立場內前四基準。若最近紀錄中有 `n` 場、`k` 場前四，則使用強度為 12 的 Beta 型縮減：

```text
posterior_top4_rate = (k + 12 × b) / (n + 12)
evidence = n / (n + 12)
recent_top4_log_signal = clip(log(posterior_top4_rate / b) × evidence × 0.16, -0.10, +0.10)
```

此設計確保即使一匹馬近期 1 戰 1 次前四，估計仍大幅回歸場內基準；而沒有符合時間閘門的歷史紀錄時，`recent_top4_rate` 為 `null`、`recent_top4_starts` 為 `0`、訊號固定為 `0.0`。

| 輸出欄位 | 含義 | 抗過擬合控制 |
|---|---|---|
| `recent_top4_rate` | 經 Beta 型縮減後的近期前四率。 | 基準由場內參戰馬數決定，先驗強度為 12。 |
| `recent_top4_starts` | 實際使用的預測前完成賽場數，範圍 0–5。 | 最新五場上限，防止長期資料掩蓋近況。 |
| `recent_top4_log_signal` | 加入強度前的對數訊號。 | 乘以證據係數並限制為 `±0.10`。 |

## 3. 資料庫、報告及可追溯性

`overseas_prerace_predictions` 已新增以下七個欄位：

| 類別 | 欄位 |
|---|---|
| 負磅 | `weight_lbs`、`field_weight_mean`、`weight_advantage_lbs`、`weight_log_signal` |
| 近期前四 | `recent_top4_rate`、`recent_top4_starts`、`recent_top4_log_signal` |

預測 JSON 同時輸出全部欄位，`feature_detail.weight` 保存公式與輸入值，`feature_detail.recent_top4` 保存實際樣本數、前四次數、場內基準、後驗率、證據係數及時間切點。這使每場 S1/S2 預測均可於賽後重建，不必以文字敘述推測特徵是否生效。

## 4. 驗證結果與限制

已執行隔離的合成契約測試 `verify_s1s2_feature_enrichment.py`。測試不聲稱任何投注或預測表現，只驗證資料流與安全條件：勝出率正規化為 100%、官方 RPR 時間閘門、T-15/T-5 完整快照、負磅訊號上限、近期前四縮減、無歷史資料的中性回退，以及新增欄位成功寫入 SQLite。

測試中的 6 馬示例以 118 磅馬匹對 124.67 磅場均負磅，得到 `+6.67` 磅相對優勢及 `+0.0267` 負磅訊號；另以 3 場、3 次前四的歷史示例，在強度 12 縮減後僅得到 `+0.00305` 的近期前四訊號。該幅度刻意很小，正是為避免把少量良好近績誤當成確定性。

> **不可把本次調整解讀為投注建議。** S1-9 僅屬單場覆盤。所有 Win／Place EV 及 Kelly 輸出仍需以同一時點的完整官方賠率、賽果歸檔及獨立的時間序列回測驗證。

## 5. 增加權重前的校準閘門

在海外 archive 累積足夠的已結算資料前，兩項上限不得調高。建議以至少 **100 場已結算海外賽事**為最低啟動條件，並以順序 walk-forward 評估而不是隨機拆分資料。

| 校準步驟 | 必要輸入 | 通過條件 | 未通過處置 |
|---|---|---|---|
| 覆蓋率檢查 | 每場官方負磅、預測前賽果、最終名次。 | 欄位缺失、同名匹配及退出標示均有審計。 | 維持中性回退，不調整公式。 |
| 時序回測 | 固定歷史切點與連續驗證季度。 | 無未來資料洩漏，並報告每季樣本量。 | 排除無法證明預測前可得性的資料。 |
| 消融比較 | 基線、僅負磅、僅前四、兩者併用。 | 機率校準與排序指標在多個驗證窗口方向一致。 | 維持現時上限，或將無效訊號設回 0。 |
| 資金檢驗 | 同步 T-15/T-5 官方賠率與已結算派彩。 | 報告 Win / Place EV、ROI、回撤及不確定性。 | 不以單期 ROI 作放大依據。 |

任何放大權重的變更，均必須連同資料截止時間、樣本覆蓋、走步驗證結果和完整版本號寫入報告與資料庫。未滿足上述條件前，本版本只把負磅與近期前四視為**保守、未校準、可審計的輔助先驗**。

# V10.2 孖T跨季度壓力測試與回撤分析指南

**作者：** Manus AI

本指南定義如何以已保存、可驗證的孖T T-15／T-5 組合快照，評估策略在不同季度的穩健性、回撤及對少數高回報事件的依賴程度。它是**歷史研究與模型監察**流程，不是未來回報保證、不是個人化投注建議，也不容許用最終賠率或賽後派彩回補缺失的賽前報價。

> 核心原則：只有候選在快照前固定、同一孖T `MAIN` 組合在 T-15／T-5 時點可定價，且其後可由官方賽果／派彩結算，才可同時計入指標 EV、實現 ROI 與回撤。

## 1. 目前資料狀態

現有專案已具備隔離測試資料與完整分析器，但尚未保存可覆蓋多個季度的**真實**孖T T-15／T-5 特定組合快照。因此，現在不能聲稱任何跨季度 ROI、最大回撤或壓力測試數字為真實歷史績效。遇到覆蓋為零或某季沒有合資格已結算候選時，報告必須輸出 `N/A`，而不是以最終市場或模擬價格填補。

| 資料層 | 最低要求 | 禁止替代物 |
|---|---|---|
| 模型候選 | 第一關開跑前已固定的兩關無順序頭三組合、模型生成 UTC 與固定研究注額。 | 賽後依結果反選的組合。 |
| 賽前價格 | 同一 `selection_key` 在 T-15 或 T-5 的 `MAIN` 特定組合顯示／估計報價，並保存原始來源與 UTC。 | 最終派彩、總池額、累積多寶、未顯示組合的推算價。 |
| 結算資料 | 兩關官方頭三、`MAIN` 正式派彩；只有無 MAIN 派彩時才可使用相符官方 `CONSOLATION`。 | 以 MAIN 報價或派彩替代安慰獎。 |
| 時間規則 | 孖T以第一關為時間錨點，`model_generated_at_utc ≤ snapshot_captured_at_utc < first_leg_start_utc`。 | 第二關或賽後資料標記為 T-15／T-5。 |

## 2. 季度定義

分析器提供兩種季度模式，正式報告必須固定其中一種，不能混合比較。

| 模式 | 季度切分 | 建議用途 |
|---|---|---|
| `hk_season`（預設） | Q1：9–11 月；Q2：12–2 月；Q3：3–5 月；Q4：6–8 月。例如 `2025/26 Q1`。 | 香港賽季內策略監察與模型重訓週期。 |
| `calendar` | 每年 1–3、4–6、7–9、10–12 月。 | 與財政、資料供應或外部研究時間表對齊。 |

每一個時點均須獨立分析：**T-15 不可與 T-5 合併成同一條 ROI 曲線**。兩者代表不同的資訊及可見價格狀態。

## 3. 關鍵輸出與壓力情境

| 指標 | 計算 | 用途與解讀界線 |
|---|---|---|
| 可定價覆蓋率 | 有同時點合資格 MAIN 組合報價的候選／已掃描候選。 | 評估 EV 是否只建立在少數可見組合上；不是命中率。 |
| 已結算覆蓋率 | 有官方結果及必要派彩的候選／可定價候選。 | 缺少結算時不輸出實現 ROI。 |
| 指標 EV／每注 | 所有可定價候選的賽前期望淨回報／固定研究注額。 | 只反映當時顯示或估計報價；平分彩金最後派彩可變。 |
| 實現 ROI | 已結算候選淨回報／已結算固定研究注額。 | 賽後歷史實現值；不可視作未來收益。 |
| 最大回撤 | 時間排序的累積淨回報峰值減當前累積淨回報之最大值。 | 顯示已實現最壞峰谷損失，不代表未來上限。 |
| 最長回撤期 | 累積曲線處於前高點以下的最長連續已結算候選數。 | 揭示策略恢復速度與序列風險。 |
| 移除單一最大正回報 | 移除一張歷史上最高淨回報候選後重新計算 ROI。 | 檢查收益是否過度依賴單次巨額派彩。 |
| 移除最佳季度 | 排除季度淨回報最高的季度後重新計算 ROI。 | 檢查跨季度穩健性，不可解讀為因果檢驗。 |
| 僅最差季度 | 單獨展示最低季度的 ROI、回撤及覆蓋。 | 壓力情境，而非策略失效的充分證據。 |

每個季度的已結算候選少於 **15** 時，標記為「探索性」；實務上建議累積到至少 **30** 張完整、同質、同一時點的候選後，才把跨季度比較用作持續模型監察。樣本門檻不會消除選擇偏差或長尾派彩風險。

## 4. 可重跑命令

### T-15：香港賽季季度

```bash
cd /home/ubuntu/hkjc_v10_database

python3 analyze_double_trio_cross_quarter_stress.py \
  --details-glob 'archive/double_trio/**/T_MINUS_15/double_trio_batch_details.csv' \
  --snapshot-label T_MINUS_15 \
  --quarter-mode hk_season \
  --min-settled-per-quarter 15 \
  --rolling-candidates 30 \
  --output-dir v102_double_trio_stress_t15
```

### T-5：曆法季度

```bash
python3 analyze_double_trio_cross_quarter_stress.py \
  --details-glob 'archive/double_trio/**/T_MINUS_5/double_trio_batch_details.csv' \
  --snapshot-label T_MINUS_5 \
  --quarter-mode calendar \
  --min-settled-per-quarter 15 \
  --rolling-candidates 30 \
  --output-dir v102_double_trio_stress_t5
```

預期輸出如下。

| 檔案 | 內容 |
|---|---|
| `quarterly_stress_summary.csv` | 每季掃描、可定價、已結算數、覆蓋率、指標 EV、實現 ROI、命中率、最大回撤與樣本狀態。 |
| `candidate_equity_curve.csv` | 每張候選的時間、固定注額、實現淨回報、滾動 ROI、累積淨回報及回撤。 |
| `stress_scenario_summary.csv` | 全樣本、移除單一最大正回報、最差季度及移除最佳季度的敏感度比較。 |
| `cross_quarter_stress_summary.json` | 輸入來源、資料品質、全局回撤、季度數、時間倒置排除數與圖表路徑。 |
| PNG 圖表 | 季度 ROI／覆蓋率、累積淨回報／回撤、壓力情境 ROI 敏感度。 |

## 5. 正式品質閘門

開始跨季度報告前，依序確認：

1. 每個候選檔案的模型時間早於其 T-15 或 T-5 快照；模型晚於快照的列必須存在排除紀錄，且不可標為 `eligible`。
2. 所有收益結算只使用批量回測器已標記 `settled=True` 的列；未結算、缺少官方派彩或缺少報價的列不可視為零收益。
3. T-15 與 T-5 分開輸入、分開輸出、分開比較；不得交叉借用價格。
4. 季度覆蓋率與已結算候選數必須與 ROI 同時展示；ROI 不能脫離樣本量與回撤單獨呈現。
5. 如輸入路徑或資料列含有 `fixture`／`test`，分析器預設拒絕。只在程式驗證時使用 `--allow-fixture`，而圖表會加上「隔離 fixture，非真實歷史」標記。

## 6. 下一步的資料收集建議

建立正式歷史樣本時，對每個孖T事件保存：未綁定候選、T-15／T-5 原始快照、綁定候選、單時點 EV 明細、官方結果及派彩來源。每日將原始檔和批量明細以賽日／第一關／時點分層保存。當累積至少四個季度且每季有足夠完整候選後，再產出跨季度壓力測試；較早階段只應報告資料覆蓋與探索性觀察。
